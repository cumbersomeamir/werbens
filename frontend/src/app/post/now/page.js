import { PostNow } from "../components/PostNow";

export const metadata = {
  title: "Post Now | Werbens — Immediate Social Posting",
  description:
    "Create and publish content immediately across your connected social media platforms.",
};

export default function PostNowPage() {
  return <PostNow />;
}
