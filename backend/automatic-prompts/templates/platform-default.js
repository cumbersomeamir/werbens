/**
 * Default - use user's priority platform from onboarding.
 */
export const DEFAULT_GUIDELINES = `
Platform: Use user's priority platform from onboarding. Format and style inferred from user data.`;